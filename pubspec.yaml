name: parsavip
description: PARSAVIP VPN
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  google_fonts: ^6.2.1
  qr_flutter: ^4.1.0
  package_info_plus: ^8.0.0
  http: ^1.1.0
  flutter_singbox_client: ^1.1.0 # پلاگین جدید برای اتصال واقعی

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
